import { DynamicSearchPipe } from './dynamic-search.pipe';

describe('DynamicSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new DynamicSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
